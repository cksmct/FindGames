# 改动记录 · 每轮「改了什么 + 多久能看到效果」

> 三份文档的分工：`README.md` 讲**怎么用**，`docs/DESIGN.md` 讲**为什么这么设计**，
> 本文件按时间记录**每轮改动清单、口径变化、验收数据、上线后的时间线**。
> 规则类改动必须三处同步（铁律 7：脚本的描述必须和实现一致）。

---

## 2026-09-25 · 判级重构（lead 优先 / 竞争实测 / 旧口径作废）+ 两个自检工具

推送：`bf2299c`（本轮 11 个功能提交 + 收尾补完，一次推送；CI 从这版代码开始跑）
涉及：`.rankup/`（本轮选题复盘产物）· `README.md` · `docs/DESIGN.md` · `package.json` ·
`src/doctor.mjs` · `src/audit-verdicts.mjs`（新增）· `src/lib/{detect,interest,serp,watchlist}.mjs` · `web/app.js`

### 1. 为什么改：三个实测事故

| 事故 | 现象 | 根因 |
|---|---|---|
| **Dressmaker**（Steam 发售 4 天 / 97% 好评 / 12,205 在线 / 畅销榜前 15） | 旧版给 **96~100 分**判「值得做」，而 SERP 上已有 **8+ 个专为该游戏新建的站**，多个在**发售前**就铺好稿 | 「上线时间」被计了两遍：新鲜度(16) 与竞争(16) 同由 `ageDays` 驱动、方向相同 → 刚上线白拿 32 分（约占三分之一权重）。而"新"恰恰是抢首发**最拥挤**的区间，不是最空的 |
| **Clash of Clans / Roblox** | 靠"专用站=0 + 需求顶级"凑出竞争 100 分，判「值得做」排到最前 | codes 前十 6+ 家全是**通用媒体**（progameguides 等对每个游戏都写 codes 页）。"专用站少"被误读成空位 |
| **lead 恒为负** | 线上 1115 条里 `lead < 0` **712 条**、`lead > 0` **0 条**，中位滞后 78 天 | 潜伏列表每轮重建、不留历史 → 转正入库时只能拿"入库时刻"当发现日 → `lead` 只实现了"惩罚晚发现"那一半 |

> 用户口径（本轮所有规则的判据）：**「我们不惧怕竞争，只是不能比别人晚太多。」**

### 2. 改了什么

**① 判级七项权重重排**（单一事实源：`web/app.js` 的 `PICK_W`，合计 100）

| 项 | 改前 | 改后 | 说明 |
|---|---:|---:|---|
| 需求规模 | 22 | 20 | 单调递增；锚点按平台分开（Roblox 终身访问 / Steam 当前在线 / 手游评分人数） |
| 内容面 | 18 | 16 | 已挖到的攻略词数量 ≈ 可直接做的页面数 |
| **发现提前量 lead** | 16 | **24（第一权重）** | 官方上线日 − 我们**最早**看到它（`firstSeenAt` 优先，没有才退回雷达入库时间 `first`）；只在游戏上线 ≤365 天时适用，老游戏跳过该项权重 |
| 竞争（分高=竞争低） | 12 | 14 | 人工核查 > 自动 SERP 实测 > 上线时长推断（**仅** >180 天的老条目）；皆无 → 未测 |
| 需求动能 | 12 | 8 | 7 天曲线后半段 vs 前半段 |
| 新鲜度（上线时长） | 10 | 8 | 只认官方上线日 |
| 口碑 | 10 | 10 | 50% 好评→0 分 · 95%→100 分 |

**② 三条硬否决（不看总分）**

| 规则 | 触发条件 | 依据 |
|---|---|---|
| 通用媒体已垄断 | 前十 ≥6 个域名、**无一专用站**、且需求分 ≥80 | 通用媒体只给有量的游戏写 codes 页，"全覆盖"本身就是长尾被占死的证据 |
| 我们晚了 | 我们首次发现日 晚于**首个专站最早快照** >30 天 | 实测两个样本：晚 17 天仍可做 / 晚 44+ 天不可做（启发式，可按样板校准） |
| 需求低于地板 | 平台绝对计数低于下限（或低于"最小参照词"的 7 天峰值） | "可测但不够"≠"未测"，如实判否 |

**③ 竞争口径修正（用户口径：对手是**新建的站**）**

- 分档输入从「前十独立域名**总数**」改为「**专用站数**」= 域名含游戏名 slug 的站（`dressmaker.wiki` ✅ · `progameguides.com` ❌）。通用站数仍如实记在 `domains` / `hosts` 里，只是不参与分档。
- **抢首发区间（≤180 天）不再用上线时长推断竞争** —— 拿不到实测就如实标「未测」并引导点「查竞争」（Dressmaker 就是这条修掉的）。

**④ lead 数据通路（把「发售前发现」的红利兑现）**

- 潜伏期首见时间持久化到 `.watchlist-firstseen.json`（点文件、不上站；保留 180 天内出现过的）；
- 转正时随队列带 `firstSeen` → `games.json` 的 **`firstSeenAt`**（`first` 语义不动：仍是"雷达入库时间"，管 30 天过期与看板「最新发现」排序）；
- **存量条目幂等回填**：线上 `.watchlist-firstseen.json` 有 94 个名字，其中 **21 个**能对回 `games.json` → 下一轮 collect 即生效；
- Steam 侧补上「**发售即转正**」：临门（0~7 天）与已发售 ≤14 天的游戏推进雷达队列（`via=watchlist-live`，免"必须有 Trends 曲线"门槛），潜伏盯了几个月的标的不会在兑现日消失。

**⑤ 需求门槛（方案 A）：自动 SERP 实测只测「值得测」的**

| 平台 | 门槛 | 说明 |
|---|---|---|
| Roblox | 终身访问 ≥ `minVisits` 1e6 | |
| Steam | 当前在线 ≥100 **或** 评价数 ≥100 | 二者取或，避免刚上线的游戏被判"没需求" |
| iOS / Android | 评分人数 ≥1000 | |
| 无官方数据（449 条） | **用内容面代替**：≥3 个可做词 | 否则这批永远过不了门槛 → 永久「竞争未测」死角 |
| 极新（上线 ≤7 天） | 放宽为「≥3 个可做词就测」 | 这段先手价值最高，而需求数据天然还没起来 |
| 🆕 老条目（>180 天） | 有 ≥3 个可做词 **且** 过需求门槛就测 | 2026-09-25 修（见 §9）。老条目的竞争分只能靠**年龄猜**（≤1 年 40 / ≤2 年 25 / ≤4 年 15 / >4 年 5），而这条猜测**永远不会被推翻**：线上 54 条长期挂着猜值，其中 8 条靠它进了可做档附近（反例 `mall game` 66 分排推荐第二） |
| 🆕 实测**优先级** | 先测**未过饱和**的老条目（亿级需求的巨头排最后） | 巨头测出来也几乎必然「通用媒体已垄断」→ 只从「未测」变「否」，不改变行动。配额有限，先花在「测了能改变结论」的地方（实测教训：只按 demand 排序时第一轮就被 Adopt Me! / Blox Fruits 吃光） |

效果：需实测从约 950 条降到**约 257 条**（按同一门槛在线上数据上估算：无上线日 121 + 上线≤7 天 25 + 其余 111）。**低需求条目长期停在「未测」是刻意取舍**，它们仍能在「🔍 竞争待核查」按待测优先度看到。

**⑥ 结构版本号：旧口径数据主动作废**（DESIGN 新增铁律 8）

- `SERP_CACHE_VERSION = 2`，判据 = 记录里有没有 `dedicated` 字段；
- 作废必须落在**所有读写点**：后端采集（缓存迁移只丢旧记录，保留已是 v2 的 19 条）· 潜伏评分（共用同一份缓存）· 前端 `compRoom` · **产物条目**（下一轮把 `games.json` 里的旧记录清掉）；
- 实测：线上 46 条缓存里 27 条旧口径、`games.json` 里 23 条旧记录 → **全部仍被更早的规则判否**，作废没有制造新的「未测」。

**⑦ 雷达分（热搜词那条腿）小修**：`volScore` 在 `vol < 1000` 时不再倒扣（钳到 0），与"识别权重比搜索量更能区分早期真游戏"的排序哲学一致；`SCORE_RULES` 同步。

### 3. 验收：同一份线上数据复算（`origin/radar-data` 2026-09-25T08:15 · 1912 条）

| 结论 | 改后（当前） | 改前（`origin/main`） |
|---|---:|---:|
| 值得做 | **2** | 29 |
| 观察 | 53 | 156 |
| 否 | 636 | 393 |
| 未测（竞争未测） | 1221 | 1334 |
| **判级发生变化** | — | **431 条（22.5%）** |

判级理由 Top（当前）：竞争未测 1203 · 发现太晚 360 · 需求低于地板 251 · 通用媒体已垄断 24 · 晚发现但需求在涨 23。
关键迁移：`244 未测→否/需求低于地板` · `23 值得做→未测`（收回白送分）· `23 否→观察/晚发现但需求在涨`（momentum 豁免）· `24 →通用媒体已垄断`（Clash of Clans / Roblox 这类事故标的）· `131 新沉入未测`（竞争不再用时长推断 + 旧口径作废，等实测补齐）。

### 4. 两个自检工具（本轮新增，随时可自查进度）

| 命令 | 看什么 |
|---|---|
| `npm run doctor` | ① 文件体检（控制字符 / BOM / 孤立 CR / markdown 围栏）② **数据体检**：产物新鲜度、字段覆盖率、`lead` 分布、以及**本地 vs `origin/radar-data`**（本地落后时会直接提示"本地复算的判级分布不代表线上现状"）。、**判级历史留档**（行数 / 行类型 / 最近一轮分布）`。`--strict` 可当门禁（退出码 1） |
| `npm run verdicts` | **判级分布复算**：用本地 `data/` 快照复算一遍判级分布 |
| `npm run verdicts:online` | 同上，但跑线上快照 + 打印分数最高的一批 |
| `node src/audit-verdicts.mjs --from-ref origin/radar-data --compare origin/main` | 同一份数据做**修复前后对照**（分布差异 + 431 条迁移明细） |
| （报告里另外固定给出） | **按上线时长的判级分布**（回答"越老是不是越不值得做"）+ **雷达窗口年龄**（`first` 超过 25 天、下几轮会滚出去的有多少） |

🛑 复算刻意**不重写第二份公式**：把 `web/app.js` **原文**装进 node 的 `vm` 复算（抄一份公式必然与前端漂移，铁律 7）。
所以这两个工具报出的数字就是页面上的真实口径 —— 改完规则先跑它们，再决定要不要推送。

### 5. 上线后多久能看到结果

前提：`bf2299c` 已在 `origin/main`，CI 每小时 `:23` 跑一轮（push 也会立刻触发一次，本轮 push 已触发）。

| 你想看到的 | 何时出现 | 依据 |
|---|---|---|
| 新判级 / 新权重 / 旧口径作废**在页面上生效** | **约 10~30 分钟**（部署完成即生效；最迟下一整点 `:23`） | push 触发 workflow → 构建部署 → CDN 缓存 10 分钟。判级是打开页面**实时重算**的，新 `app.js` 一上线，分数与排序立刻变 |
| `firstSeenAt` 回填 → `lead` 出现正分 | **≤1 小时**（下一轮 collect） | 线上可回填 21 条；本地一轮实测回填 8 条 |
| `games.json` 里的旧口径竞争记录被清掉 | **≤1 小时**（下一轮 collect） | `enrichSerpComp` 的条目层作废 |
| 竞争实测**开始出结论**（新的「值得做 / 可小试」） | **每小时都在补**（每轮最多 12 条实测）→ 头一两轮通常就有新结论 | 当前线上已有 2 条「值得做」（如 The Hunt: Roblox 20，87 分） |
| 竞争实测**铺满** 257 条待测 | 乐观 12 条/轮 → **约 21 小时（≈1 天）**；若 DDG 限流回到 ~2 条/轮 → **约 5 天** | 本机实测 DDG 本轮 12/12 成功；代码注释里的保守值是 ~2 条/轮 |
| 「可做页面的词」 | 与游戏**同时**产生（曲线接口每个游戏返回约 12 个攻略词），不需要额外等待；词池已到 `pool.maxItems` 上限 20000 | `interest.mjs` / `pool.mjs` |
| 🆕 留存策略 / 体积护栏生效（谁退出、为什么） | **下一轮 collect（约 1 小时）**，且退出会留痕在 `.verdict-history.jsonl` 的 `exit` 行 | `games.retention` / `maxItems` |
| 🆕 判级历史可查（某个游戏/整体怎么变） | **第一轮 collect 后**就有（首轮记「进来就可做」的条目 + 一行分布），越攒越长 | `lib/verdict-history.mjs` |

**一句话**：页面上的判级与排序**几分钟内**变；`lead` 与旧口径清理 **1 小时内**到位；
竞争实测 **1 天（乐观）~5 天（限流时）**铺满；**「值得做 + 它的可做词」从第一轮实测起就持续冒出来，不用等铺满。**

**想更快（按性价比排序）**

1. **配 `BRAVE_API_KEY`**（免费 2000 次/月；把 `config.json` 的 `games.serpComp.provider` 改成 `brave`）→ 摆脱 DuckDuckGo 限流，`maxPerRun` 可从 12 调高（257 条 → 每轮 30 条 ≈ 9 小时铺满）；
2. 调高 `games.maxCurvesPerRun`（默认自适应 24~120）与 `catalogDirect.maxPerRun`（60）→ 每小时被评估的候选更多（代价：Trends 限流风险）；
3. **不建议**放松 `serpComp` 的 `minVisits` / `minPlaying` / `minRatings` —— 那只会把低需求条目拉进排队，**不会更快出结果**。

**每天的自查动作**：`npm run doctor`（数据新鲜度、旧口径是否清零）+ `npm run verdicts:online`（判级分布变化）+ 打开看板「🎯 建站推荐」按「🚀 首发窗口」筛。

### 6. 遗留与已知边界（诚实交代）

1. **「未测」占 64% 是刻意取舍**（当前线上 1221 / 1912）：低需求条目过不了实测门槛 → 长期停「未测」。它们**不会静默消失** —— 看板「🔍 竞争待核查」按"待测优先度"列着（固定分母 100：缺项多 = 证据少 = 不优先看）。
2. **CDX（首个专站出现日）本轮 12 次几乎全部超时**（Wayback 限流）→ `ourLagDays`（我方滞后乘数）基本没生效；拿不到就 ×1，不猜、不罚。
3. **队列 1820 条且在净增**（来源每天产出几百个候选，验证名额有限）→ 队列不会"清空"，瓶颈是每轮配额而不是积压。想一次性冲刺：`node src/collect.mjs --only-games --max-curves 500`。
4. **CI 跑在云机房 IP 上**，DuckDuckGo / Google 的限流表现可能与本地不同；线上跑几轮后才能定 `serpComp.maxPerRun` 的稳态值（本机 12/12 成功是乐观侧数据点）。
5. **本工作区的写入陷阱**：IDE 的文件写入工具在 `d:/Source/Skills/findnews` 里**不落盘**（报成功，但 shell / git / node 看不到）。改文件请用真实 IO，例如
   `[System.IO.File]::WriteAllText($path, $text, (New-Object System.Text.UTF8Encoding($false)))`，改完用 `node --check` + `git diff` 复核。


### 7. 生命周期与留存：退出机制已改成「事实性退出」（2026-09-25 实测）

**机制上，随时间变的只有三项，其余不随时间衰减**：

| 项 | 随时间怎么变 | 备注 |
|---|---|---|
| 新鲜度（权重 8） | **单调下降**：≤30 天 100 → 1 年 40 → 5 年 10 → 10 年 0 | 唯一"不看采集也会变"的项 |
| 竞争（权重 14） | ≤180 天**给不出分**（标未测）；>180 天起用上线时长推断：≤1 年 40 → ≤2 年 25 → ≤4 年 15 → >4 年 5 | 所以它先是"判不了"，之后才缓慢变差 —— **不是平滑下降** |
| 发现提前量 lead（权重 24） | **数值恒定**（= 官方上线日 − 我们首见日，是个常量）；只有上线 >365 天后**整项跳过** | 它不是衰减项，是"进场早晚"的一次性事实 |
| 需求规模 / 内容面 / 口碑 / 动能 | **不随时间衰减**（访问量、评价只增；动能有独立的转凉闸） | 动能靠曲线保鲜：连续 2 次重取无量 → 降「观察」档 |

**真正的"退出推荐"**留存策略（2026-09-25 改：退出必须是事实性的，不是时间性的）**

| 层面 | 现在的规则 |
|---|---|
| **留存** | **任一条成立就留着**：`last` 在 `signalDays`(30) 内（还在被看到）· 有曲线且未转凉 · 有 ≥3 个可做词 · 有未过期的当前口径竞争实测 · 未发售 / 潜伏转正 · 人工核查过 |
| **退出** | 只有「**连续 30 天没有任何新信号** 且上述全不成立」才退出 —— 且**退出即归档**（`exit` 行写明 why） |
| **体积护栏** | 超过 `maxItems`(3000) 时按**可做性 / 信息价值**降序淘汰（官方数据 +2 · 曲线 +2 · 可做词 +2 · 实测竞争 +3 · 人工 +3 · 潜伏 +2 · 转凉 −1），时间只作次序 |
| **排序** | 前端仍按 `first` 倒序（「🆕 最新发现」）—— 「新」是排序偏好，不是留存判据 |
| **首见时间** | 名字级首见表登记**所有雷达条目**，退出后再被重新发现时 `firstSeenAt` 能恢复 → `lead` 不受记账影响 |

**旧规则（已废弃，别再改回去）**：按 `first` 30 天硬删 —— 把「我们的记账时间」当成「游戏的有效期」，
会无声删掉「31 天前发现但仍有戏」的条目，并让它日后以新的 `first` 重新开始（曲线 / 竞争实测 / 首见全丢）。
**线上实测**（1912 条 · 2026-09-25 快照 · `node src/audit-verdicts.mjs --from-ref origin/radar-data`）：

| 上线时长 | 条目 | 值得做 | 观察 | 否 | 未测 | 有总分 | 平均分 |
|---|---:|---:|---:|---:|---:|---:|---:|
| ≤7 天 | 627 | 0 | 1 | 236 | 390 | 1 | 57 |
| 8~30 天 | 54 | 1 | 0 | 8 | 45 | 2 | 68 |
| 31~180 天 | 377 | 0 | 21 | 348 | 8 | 4 | 63 |
| 181~365 天 | 23 | 0 | 2 | 21 | 0 | 13 | 44 |
| 1~3 年 | 70 | 1 | 16 | 3 | 50 | 20 | 52 |
| 3~5 年 | 39 | 0 | 6 | 3 | 30 | 9 | 48 |
| >5 年 | 58 | 0 | 6 | 1 | 51 | 7 | 47 |
| 无官方上线日 | 664 | 0 | 1 | 16 | 647 | 0 | — |

读法（**不是单调下降**）：

- **≤30 天**：分不低，但**几乎出不了「值得做」** —— 极新的游戏大多"竞争未测 + 需求还没起来"，这正是 Dressmaker 修正后的预期行为（「新」≠ 空位）；
- **31~180 天**：否的比例最高（`发现太晚` 硬否 + 竞争未测）；
- **181 天 ~ 1 年**：平均分掉到 **44**（新鲜度衰减 + lead 仍为负 + 竞争只能给 40 档）；
- **1 年以上**：平均分回到 **47~52**（lead 整项跳过、老游戏的访问量与口碑更高）→ **"老"本身不会判死**；
- **无官方上线日 664 条（35%）**：拿不到上线日 → 竞争无从推断 → 647 条停在「未测」，只能等 SERP 实测或人工核查。

**留存策略实测（本地一轮）**：留存退出 0 条（`last` 都在 30 天内）✓；把上限临时压到 1200 时，体积护栏按可做性淘汰 160 条
（可做档逐条记 `exit`、其余聚合 `exit-many`），保留集里「有官方数据 767 / 有曲线 294 / 有可做词 129 / 有实测竞争 49」。
线上快照（1912 条）里 `first` 全在 7 天内、`last` 中位 **3.5 小时** —— 系统只跑了约一周，
所以「事实性退出」的真实效果要等 **10 月中旬**（第一批条目的 `last` 开始过期）才观察得到。
### 8. 历史留档：现在有什么（方案 1 已实施 · 2026-09-25）

| 数据 | 留档 | 保留期 | 说明 |
|---|---|---|---|
| 热搜词 `history*.json` | ✅ **有** | 7 天（按 `last`） | `(词, 地区)` 去重；带 `vol_peak` / `growth_peak` / `first` / `last` / `sightings` |
| 关键词池 `keywords.json` | ◐ 半有 | 7 天（按 `last`） | 每个词带 `first` / `last` / `count` |
| **游戏雷达 `games.json`** | ❌ **没有** | 只有**当前窗口**（`first` 30 天内 + `maxItems 3000`） | `writeGames` 整份覆盖，不存快照 |
| `radar-data` 分支 | ❌ **没有** | 每小时 `--force` 单提交覆盖 | 刻意换"仓库体积恒定"，代价是过去的小时级数据全部丢弃 |
| 名字级首见 `.watchlist-firstseen.json` | ✅ 有 | 180 天 | 事后**唯一**能回答"我们何时第一次看到它"的地方（`firstSeen` / `lastSeen`）；只覆盖在潜伏清单出现过的名字 |
| 竞争实测 `.serp-cache.json` | ◐ 半有 | 60 天 | 每条带 `at` / `domains` / `dedicated` / `competitorFirstSeen` |
| 队列 `.queue.json` | ◐ 半有 | 21 天 TTL | 只剩"还没验证"的条目，验证完即出队 |
| 条目内时间戳 | — | 随条目 | `first` / `firstSeenAt` / `chart_at` / `statsAt` / `serp.at` / `coolStreak` |

**结论：没有"这个游戏上周多少分 / 当时判成什么"的历史。**
能重建的只有"**当时那份数据 + 当时那版代码**"下的结果（`node src/audit-verdicts.mjs --from-ref <旧 ref> --compare <旧提交>` 这类复算），
而数据本身没有时间序列 —— 除非当时有人把它存下来。热搜词是唯一例外（7 天留档，能回答"这个词什么时候第一次出现、峰值多少"）。

**方案 1 已实施（2026-09-25）：`data/.verdict-history.jsonl` + `data/.verdict-state.json`**

- **只记变化**：`dist`（每轮一行分布）· `verdict`（判级升降，**仅涉及可做档 yes/warn**）· `comp`（未测 → 实测）·
  `enter`（进来就可做的）· `exit` / `exit-many`（退出：可做档逐条记、其余聚合计数 + 采样 5 个名字）· `trim`（超上限滚最早一段）。
  🛑 不记全量：1900 条 × 每轮 ≈ 300KB → 一天 7MB，会把 force-push 的分支承爆。实测首轮 48 行（47 条可做档新进 + 1 行分布）、次轮 12 行（9 条新实测竞争 + 1 条判级变化 + 1 行分布）。
- **状态侧车** `.verdict-state.json`（约 60KB / 1238 条）：只用来算「这轮变了什么」，本身不是档案。
- **退出留痕**：留存策略与体积护栏给出的退出原因写进 `exit` 行的 `why`（如 `容量护栏（可做性最低）` / `no-signal-30d`）。
- **首见时间跨轮保留**：名字级首见表现在登记**所有雷达条目**（本地实测一轮新登记 1230 条）→ 条目退出后再被重新发现时 `firstSeenAt` 能恢复，`lead` 不会因为我们的记账变负。
- **上限** `games.history.maxLines`（默认 40000 ≈ 6MB），超限滚最早一段并记一行 `trim`。
- **查看**：`npm run doctor` 报行数 / 行类型 / 最近一轮分布。
- 方案 2（每天留一份 `games.json` 全量快照）**没做** —— 方案 1 已能回答「变了什么、为什么变」，全量快照的体积代价大得多（需要单独的体积护栏）。

---
### 9. 事故复盘 ②（2026-09-25）：推荐页第一、第二名都是假信号

用户在页面上看到的头部（`Roblox` 排第一、`The Hunt: Roblox 20` 排第二）**都不该在那里**。拆开数据是两个独立缺陷。

#### ① `Roblox`（87 分 · 值得做 · 推荐第一）—— 官方域名被当成了「专用站」

- 它的 SERP 记录：`domains 8` · `dedicated 1` · `dedicatedHosts [roblox.com]`。
- 判据 `isDedicatedSite(site, name)` = 「域名里含游戏名 slug」→ `roblox.com` 含 `roblox` ✅ → 被算成「专为它建的站」。
- 连锁：`dedicated = 1` → 「通用媒体已垄断」硬否**失效**（那条要求 `dedicated === 0`）→ 竞争档从「0 个 = 100 分 / 直接否」变成「1~2 个 = 75 分」→ 需求 100 + 内容面 100 + 动能 52 + 口碑 85.5 → **87 分排第一**。
- 对照：`Clash of Clans` 的 `dedicated = 0` 就正常触发硬否（分数 93 仍判否）—— 说明**只要混进一个自指域名，整条判据就失效**。
- 更深一层：`Roblox` 这个条目本身是**平台泛词**（Google Play 直收进来的），而来源型条目原先整段跳过重筛（`if (g.src) return true`），AAA 名单里的 `roblox` 因此永远清不掉。

#### ② `The Hunt: Roblox 20`（87 分 · 值得做 · 推荐第二）—— 限时活动白拿「早发现」奖励

- 它是 9/17–9/28 的限时活动，用户截图时只剩 3 天；页面早就有 ⚠️ 提示，但**只提示、不扣分**。
- 而且它叠了 **×1.15**：`ourLagDays` 算的是「我们比首个专站晚了几天」，而**活动类站点几乎都是活动开始才建**
  （`the-hunt-roblox20.wiki` 最早快照 2026-09-20 = 活动开始后 3 天）→ `ourLagDays ≈ 0` → 「我们没晚」**白送奖励**。
  反讽之处：越是一次性活动，越容易拿到「先手」加分。

#### 修法（本次改动，均已实测生效）

| 缺陷 | 修法 |
|---|---|
| 自指 / 官方域名 | `PLATFORM_OWN_DOMAINS`（roblox.com · steampowered.com · play.google.com …）**不算专用站**；名称本身就是平台/渠道时（Roblox / Steam / Poki）任何域名都不算专用站。缓存与产物里的旧记录由 `deriveComp()` / 前端 `serpDerived()` **就地重算**（不必等 7 天 TTL）—— 见 DESIGN 铁律 10 |
| 平台泛词进库 | AAA 名单对**来源型条目也生效**（原先只拦新候选：`Roblox` 是 Play 直收进来的，进库后再没机会被清） |
| 限时活动 | 名字命中 `the hunt / event / season N / update N` → **不进可做档**（降到「观察」并写明理由）；且不给「我们没晚」的正奖励（`m > 1 → 1`） |
| 老条目靠年龄猜竞争 | 老条目（>180 天）不再一律跳过实测：有 ≥3 个可做词且过需求门槛就测（线上 54 条） |
| 实测优先级 | 先测**未过饱和**的老条目，亿级需求的巨头排最后 —— 否则 12 条/轮的配额第一轮就被 Adopt Me! / Blox Fruits 吃光，真正靠猜值撑在可做档的条目永远轮不到 |

#### 验收（同一份线上快照 · 2132 条）

| 标的 | 修前 | 修后 |
|---|---|---|
| `Roblox` | 值得做 · 87 分 · **推荐第一** | **否 · 通用媒体已垄断**（记录里留痕 `ownExcluded: [roblox.com]`） |
| `The Hunt: Roblox 20` | 值得做 · 87 分（含 ×1.15）· **推荐第二** | **观察 · 限时活动（不进可做档）**，乘数回到 ×1.00 |
| 一轮实测选中谁 | 12 条全是 Adopt Me! / Blox Fruits / Piggy 这类巨头 | 12 条变成 `mall game` / `town` / `Decaying Winter` / `Kaizen` 等「靠年龄猜竞争、撑在可做档」的条目 |
| 当前可做档 | — | `yes 2`（Scream And Run 67 · mall game 66，后者等实测替换猜值）；分布：否 739 · 观察 52 · 未测 1339 |

### 10. 事故复盘 ③（2026-09-25）：雷达头部「值得建站吗」与「US 地区却非英文」

用户看「新游戏雷达」按分数排的前几名（`edition collector gta 6` · `xbox studio changes and restructuring` · `24 de septiembre` · `santa fe x deportivo cali` · `disney dreamlight valley update`）问了两个问题，**两个都是真问题**。

#### ① 「这些真的值得建站吗？」—— 页面自己的定义就不是「可做性」

雷达分（`score`）在页面「评分规则」与 README 里都写明是**验证优先级**：
`log₂(搜索量/1000)×2 + 涨幅%÷100 + 起飞档 + 识别权重×2 + 人工加分` —— 它回答「先验证哪条」，不回答「值不值得建站」。**可做性在「🎯 建站推荐」页**（七项加权 + 硬否决）。
但话虽如此，上面那几条**根本不该进雷达**：它们要么不是游戏作品，要么不是英文词。

#### ② 「为什么区域是 US，词却不是英文？」—— 地区标签是「我们取曲线的地区」，而且曲线被静默回退过

- 卡片上的 US 是 `chart_geo`（**我们取曲线用的地区**），不是「这个词在哪儿热」—— 真实上榜地区在 `geos` 里。
- 更糟的是取数逻辑：`prefGeos.has(c.geo) ? c.geo : US` —— 词只在 CL/FR/DE/VN 热时**静默回退到 US 取曲线**，
  于是算出一条与这个词无关的曲线、还贴上 US 标签。实测 59 条（2.7%）。
- 根因是两道闸缺失：`latinOnly` 只排非拉丁**字母体系**（西/德/法/葡/土全是拉丁字母），而**准入地区闸**等于没有。

#### 修法（本次改动）

| 缺陷 | 修法 | 线上复算影响（2220 条） |
|---|---|---|
| 非英文（重音 / 日期 / 功能词） | `detect.mjs` 新增 `englishOnly` 闸 + `nonEnglishEvidence()`（可解释，默认开） | 清 43 条 |
| 非拉丁脚本漏进来源型 | 英文闸把非拉丁脚本一并纳入，且**对来源型条目也生效**（原先整段跳过重筛） | 清 18 条 |
| 只在小语种地区热 | **准入地区闸** = `games.geos`（英语六国）；曲线不再静默回退 | 清 34 条 |
| 新闻 / 法律 / 公司事件 | `NEWS_ONLY`（settlement / lawsuit / restructuring / merger / earnings / ipo …） | 清 3 条 |
| AAA 品牌变形 | `AAA_SUBSTR`（**包含式**，只用于热搜词）：`edition collector gta 6` 这类整串锚定抓不到 | 清 8 条 |
| 体育对阵的拉丁式 `x` | `FIXTURE_FORM` 增加 `\bx\b`（仍要求 Sports 分类同时出现） | 覆盖 `santa fe x deportivo cali` |
| 显示口径混淆 | 卡片改为「**热于** <geos> · 曲线地区 <chart_geo>」 | — |

合计清掉 **112 条（5.0%）**；热搜型从 79 → 23（剩下的确实是英文 + 英语区 + 非 AAA/新闻）。本地冒烟 `EXIT 0`。

#### 两个「不是 bug 但要知道」

1. **雷达分低 ≠ 不值得做，雷达分高 ≠ 值得做** —— 排序键是「验证优先级」。判断可做性请用「🎯 建站推荐」页（两页现在都会显示 `热于`）。
2. **`latinOnly` 的注释原来写着「`New Pokémon Snap` 能正常通过」** —— 加了英文闸后这句不再成立（重音字母会被判非英文），README 已同步改口（铁律 7）。

### 11. 分数明细 + 作品名归一化（2026-09-25）

用户提了两件事：① 想把每个游戏的分数**具体怎么来的**标出来（好对算法给反馈）；② 为什么有游戏名带逗号（`Dear Fridge,`）。

#### ① 分数明细：两个分数都拆开，且只有一份公式

| 分数 | 归属 | 明细怎么来 |
|---|---|---|
| 雷达分 | 后端 `detect.mjs` | 抽出 `scoreBreakdown()`（`scoreKeyword` = 它的 `total`，公式仍只有一份），返回**逐项得分 + 原始输入**，随条目落盘为 `scoreParts` |
| 可做性分 | 前端 `web/app.js` | 新增 `pickDetail()`：每维「值 × 权重 = 贡献」→ `Σ ÷ Σ权重` → `× 乘数` → 总分；缺项如实标「缺项（未计入，不是 0）」 |

- 两个页面各加一个折叠块「分数明细（score N 怎么来的）」；字段没回填的老条目**如实说明**「等下一轮采集」而不是显示 0。
- 顺带修一个标注错误：`rankability()` 原先 `manual.mult` 存的是**总乘数**（含我方滞后），卡片却写成「发稿滞后乘数」——
  两个乘数同时生效时那个数字对谁都不成立。现在拆成 `manualMult`（对手发稿滞后）与 `lag.mult`（我方晚于首个专站），明细里分开列。
- 验收：`node src/audit-verdicts.mjs --breakdown 12`（`npm run verdicts:breakdown`）逐项打印，
  并对前端两个明细块做 **HTML 渲染冒烟**（本次 6 条样本全部 ✓，见 README「分数明细」一节的真实输出）。

#### ② `Dear Fridge,` 的逗号：**是开发者自己写的名字**，但尾标点该去掉

itch 原页标题是 `Dear Fridge, by Magister Waldemar`（itch 标准格式「作品名 by 作者」）→ 作品名本身就是
`Dear Fridge,`。我们**不编造名字**，但尾标点在搜索里没有任何意义（作品名是 Trends / SERP / 页面词的查询键），
所以新增 `normalizeName()`（`src/lib/sources.mjs`）：

- 只去**尾部**的 `, ; : 、` 与多余空白；`Go, Bobby, Go!` / `I, The One` 这类**内部**逗号保留（是名字的一部分）；
- 入库前在 `collectSourceCandidates()` **只做一次**，`rawName` 保留原始标题备查；
- 读 `games.json` 时也归一化一次 —— 否则旧条目（`Dear Fridge,`）会以旧键留着，来源下一轮报新名 → 同一游戏两条记录。

### 12. 缺项也给分：口径定稿 + 提前量改加分项 + 细项排名（2026-09-25）

用户提了三条：①「绝大多数条目给不出总分」是最严重的问题 —— 数据缺失先略掉但标明；② 提前量改成加分项、没有就 0 分；
③ 希望能按某个**细项**排名。三条都做了；但①按用户字面方案（归一化）实现后**当天就被复算否掉**，最终口径改了。

#### ① 缺项怎么算：**中性值 50 补进固定分母**（不是“按有值维度归一化”）

先照字面实现（归一化）并复算，结果：

```
条目 2217 → 给出总分 1741（覆盖满意），但最高分那一档变成：
  100  未测  TurboNinja Preview v1.0 / Experiment Virtual Physics Simulation / Sapamuk (Demo) / Dear Fridge, …
```

原因：归一化下“只有一维有值、且恰好是 100”就等于满分（这些 itch 小游戏只有“刚上架”这一项），
与本文档早先记过的 `sony playstation / gta 6`、`preScore` 注释里的 `Caravan SandWitch` 事故**同形**。

定稿口径：

| 情形 | 算法 | 卡片标注 |
|---|---|---|
| 缺**非竞争**维度 | 缺项按**中性 50** 计入**固定分母** `PICK_W_TOTAL`(76) | 角标「参考」+ title/明细列出缺项 |
| 缺**竞争** | 同样按 50 计入，但那个数字只是**上界** | 角标「上界」；**结论仍是「竞争未测」**，不升级为值得做 |
| 有值维度 **< 2** | **不给分**（沉底） | 结论「数据不足（判不了）」+ 写明缺了哪几项 |

实测（线上 2217 条，同一份数据）：给出总分 **88 → 821 条**；判级分布 **完全不变**（yes 3 / warn 53 / no 812 / unknown 1349）
—— 即“缺项给分”只让**排序**可用，不改变**结论**；最高分档恢复为实测过竞争的真游戏（Dressmaker 98 / Steal An Egg 97 / Ride A Pet 93）。

**剩下 1396 条为什么仍不给分**（实测诊断，结论是“别再用放宽规则解决”）：

- 476 条 **0 维有值**（无官方数据 / 词 / 曲线 / 上线日）；920 条 **1 维有值**（其中 856 条只有“上架日期”一项）；
- 配额瓶颈：竞争缺 2118 条（自动 SERP 每轮最多 12 条 ≈ 176 轮 ≈ 7 天）· 内容面缺 2027 条（相关词随曲线才取）· 需求规模缺 1567 条（itch/poki 无官方计数接口）。

#### ② 发现提前量改成加分项（0 ~ +12）

原为**权重最高**的一维（24/100），但可算的 1525 条里 `lead>0` 只有 **7 条（0.5%）**、中位 **−4.6 天** —— 对绝大多数条目只是恒定的低分拖累。
现在 `leadBonus = round(leadScore × 0.12)`（100→+12 · 90→+11 · 80→+10 · 55→+7 · 35→+4 · 15→+2 · 5→+1），
**无数据 / 上线超 1 年 → +0**；加权维度 7 → **6**（Σ=76）；晚发现的惩罚不重复罚（`lag` 乘数 + 新鲜度已承担）。

#### ③ 细项排名

推荐页排序条新增：**按可做性分 / 内容面 / 需求规模 / 竞争 / 需求动能 / 口碑 / 提前量加成 / 待测优先度**（缺项沉底）。
用途：核对“某一项的打分是否合理” —— 按内容面排，一眼看最强的那批是不是真的内容面强。

验收：`npm run verdicts:breakdown` 逐项打印（算式与分数逐行对得上）并对前端明细块做 HTML 渲染冒烟（本轮样本全部 ✓）。

### 13. 提前量加分：上限 12 → 25，且乘「流量因子」（2026-09-26）

用户看图后提两点：①「发现提前量这一项数量级和别的不一样，虽然是加分项但也太低了 ——
一个游戏如果能**提前发现、还具备一定的流量**，大概率就是机会」；② 顺带问「识别权重 / 人工加分」是什么
（已写进 README 新增的「🧩 雷达分五项各是什么」）。

#### 改动（`web/app.js`）

- `LEAD_BONUS_MAX`：12 → **25**；新增 `leadDemandFactor(demand)`；
- 算式：`leadBonus = 25 × (leadScore / 100) × 流量因子`；
- 流量因子：需求 ≥80→1 · ≥60→0.85 · ≥40→0.6 · ≥20→0.35 · <20→0.15 · **需求缺→0.5（中性）**；
- 明细表把算式写进那一格（`上限 25 × 流量 0.85`），角标 title、规则块文案同步。

🛑 **为什么乘流量、而不是一律给满**：**先手本身不是机会，「先手 + 有人搜」才是** —— 线上 ≤7 天的新条目 810 条占大头，
一律给满会让「早发现的死游戏」靠加分挤进推荐。（上一版被否掉的归一化事故是同一类错误：把“缺证据”当“满分”。）

#### 实测（同日对照，同一份线上数据）

| 条目 | 提前量 | 旧加分 | 新加分 |
|---|---|---|---|
| Dressmaker | 上线后 4 天 | +7 | **+14** |
| The Hunt: Roblox 20 | 上线后 6 天 | +7 | **+14** |
| Build the Pyramid!（用户截图的例子，需求 60） | 上线后 17 天 | +4 | **+7** |
| Steal An Egg | 上线后 57 天 | +2 | +4 |

判级分布**迁移 0 条**（yes 3 / warn 53 / no 812 / unknown 1349 不变）—— 这次只是把刻度摆正，
没有把不该进「值得做」的条目推上去（硬否决与阈值都没动）。

### 14. 雷达分重做：流量主导 + 动能去重 + 删掉识别权重与人工加分（2026-09-26）

用户四条：① 识别权重与人工加分"没用"，全部去掉；②「流量是最重要的参考，增幅和起飞都没流量重要」；
③ 怀疑涨幅与起飞重复；④ 明确要求「grill me」。

#### 先 grill（每条都给实测，不给观点）

| 用户的说法 | 数据怎么说 | 结论 |
|---|---|---|
| 识别权重没用 | 对**来源型条目它是恒定 3**（固定 +6 分，排序上是常数、零信息）；对热搜词只有 2~5 的粗分档 | **同意删掉计分**；但**准入闸保留** —— 低量区（vol<1000）要求权重≥3，实测每轮挡掉约 41 条垃圾（`paddy power` / `bwin bonus` / `the sun` / `mediathek ard` / `eurojackpot` …） |
| 流量最重要 | 按 vol 直接降序取 Top 20（非噪音 + 过闸）= `xs mn`(20 万) · `demo hari ini` · `krafton cổ phiếu` · `septembre` · `18 settembre` · `23 september` · `un million` · `số miền nam` —— 彩票 / 日期 / 非英文 / 事件词，正是 9/25 第一条反馈抱怨的那批 | **部分同意**：流量当**主项**（30/42），但不能当唯一轴（要配合语言/地区/赛事/新闻四道闸 + 动能） |
| 涨幅与起飞重复 | 池子非噪音 7468 条：搜索量 vs 涨幅 Spearman = **0.453**（中度重复）；涨幅 vs 起飞 = **0.23**（样本仅 21 条，不足） | **部分同意**，但重复的是「涨幅 vs **搜索量**」而不是「涨幅 vs 起飞」→ 处理办法是"二者**取最大、不叠加**计分"，不是删掉 |
| 人工加分没用 | 线上 `feedback.boost` 一直是空 → 恒 +0 | **同意删掉**；`feedback.block` 的**否决**保留（那是另一回事） |

#### 改动

- `src/lib/detect.mjs`：`scoreBreakdown({vol, growth, hype, official})` ——
  `流量 = max(搜索量分 0~30, 官方量级分 0~30)`、`动能 = max(起飞档, 涨幅档) → 0~12`；删掉 `weightScore` / `feedbackBoost`；
  新增 `officialDemandScore(stats)`（与前端 `demandScore` **同锚点**的后端镜像，两处注释互相指路）；
  `FEEDBACK_BOOST_PTS` 标 `@deprecated`（值改 0）；`SCORE_RULES` 自述同步重写。
- `src/collect.mjs`：算分时传入官方量级；**官方数据 enrich 之后重算一次**雷达分
  （来源型条目在算分那一刻还没有 stats，不重算就永远停在"只有动能"的低分区）；只对"官方量级变了"的条目重算（幂等）。
- `web/app.js`：雷达明细块拆成「流量（搜索量 / 官方量级 / 小计取最大）+ 动能（起飞 / 涨幅 / 小计取最大）」，
  并加说明"识别权重不再计分、人工加分已去掉、动能取最大"；旧口径条目（无 `traffic` 字段）**如实标成旧口径**，不假装是新口径。
- 顺手修一条漏网：`xs mn`（越南彩票 xổ số miền nam 缩写，原正则 `xs(mb|mn|mt)` 要求无空格）→ `xs\s*(mb|mn|mt)`。

#### 实测

- 判级分布**迁移 0 条**（yes 3 / warn 53 / no 812 / unknown 1349）—— 雷达分与可做性分相互独立，改它不动结论。
- 来源型条目首次有区分度（本地实测）：`Vita Mahjong`（完全无 Trends 数据）score 33 = 流量 30（官方 100）+ 动能 3；
  `Bus Fever Party!` 26 = 流量 14.2（官方 47）+ 动能 12。（改动前它们恒在 6~14。）

### 15. 搜索量口径修正：改用 7 天峰值，而不是本轮采样桶值（2026-09-26）

**用户报告**：`control resonant` 在 Google Trends「流量非常猛」，为什么分数还是 0？

**查证**（拉最新 radar-data 逐字段核对）：

| 数据源 | 该词的值 | 说明 |
|---|---|---|
| `keywords.json`（词池） | `vol: 10000`，`growth: 1000`，`count: 82`，8 个地区在榜 | 用户看到的「猛」有事实依据 |
| `history.json`（7 天留档，按 (词,地区)） | **US `vol_peak: 10000`**，最近一次上榜 2026-09-26 02:31 | 峰值确实是 10K |
| `games.json`（条目） | `scoreParts.vol: 1000` → `volScore 0` | **算分用的是本轮采样值 1K** |

- **根因**：Google 的 `vol` 是**分桶值**（`100/200/500/1K/2K/5K/10K/…/2M`），
  单轮单地区的采样会落在不同桶上；而算分只取 `fresh` 里**本轮**那一桶（候选构造时 `...it` 带的就是本轮值），
  留档里的 `vol_peak` 从未参与算分。于是「一轮采样偏低 → 搜索量那项归零」，
  同时池子页显示峰值 10K、卡片算分用 1K —— 两边口径不一致。
- **修法**：算分改用 **`max(本轮桶值, 7 天留档峰值)`**（`collect.mjs` 建 `peakOf` 表 + 候选带 `volPeak`/`volRound`），
  明细里把它写成「7 天峰值；本轮采样 1K」，不再让人误以为"这个词没量"。
  分数不该因为某一轮采样偏低而归零 —— 这也是「低量不倒扣」同一条原则的延伸。
- **实测**：`control resonant` 复算 `volScore` 0 → **9.1**；但它的 **总分仍是 38** —— 因为流量项取的是
  `max(搜索量, 官方量级)`，而它的官方量级（Steam 评价数）已经顶到 30/30 满分，搜索量这一项不再是瓶颈。
  换句话说：**这次修的是"那一行显示 0"的误导**，对**没有官方数据的 Trends 型条目**才会真正改分
  （线上目前只有 2 条按新口径算过分，样本太小，下一轮 CI 覆盖后可再量化）。


#### 全量实测（2026-09-26 补，含更正）

用户追问「不能只改这一条，其他很多应该都错了吧」——我用**全量留档**（`history.json` + `history-p2..p9.json`，共 **25499 条**，注意留档是分片存的，只读首片会得到假低的命中率）逐条核对：

| 面 | 数字 |
|---|---|
| 当前窗口在榜词 | 2858 条（留档 **100%** 能查到各自地区的峰值） |
| **本轮搜索量 < 同地区 7 天峰值（被低估）** | **345 条（12.1%）** —— 2× 201 · 5× 72 · 10× 66 · 100×+ 6 |
| 涨幅同类低估 | 366 条 |
| 这些词里能**过游戏闸门**（真会进雷达）的 | **只有 2 条**（其余是体育/新闻/非英文等噪音，本来就进不来）· 单条最大提分 3 |
| 线上雷达 3000 条：能对到留档的 Trends 型 | **29 条**；来源型 2968 条 —— 名字来自目录（Roblox/Steam/应用商店），**根本不在热搜里，不涉及这个 bug** |

🛑 **更正**：本文档上一版说「33.8% 的在榜词被低估」——那个数字是拿**词池**（存的是**跨地区 + 跨时间**的最大值）比出来的，
把"地区差"也算成了"时间差"。按**同地区**峰值重算是 **12.1%**。口径不同，结论差 3 倍，这里留档更正。

**所以这次修的是"一类真实失真"，不是"很多条目都错了"**：它影响 12% 的在榜词（含 6 条低估百倍），
但其中能进雷达的极少；收益主要是两点 —— ① 榜单与池子页口径一致（都按峰值），不再自相矛盾；
② 对未来入库的热搜词 100% 生效（候选构造时即取峰值），已入库条目在下一轮重算时生效。

**同时修了涨幅**：`growth_peak` 与 `vol_peak` 一样存在留档里却从未参与算分（366 条在榜词被低估），
现在两者都取 `max(本轮, 7 天峰值)`，明细里分别标注「峰值；本轮 X」。

📌 **顺带记一个留档本身的性质**（排查时踩到）：历史留档是**分片**写的（`history.json` 是第 1 片，
其余在 `history-p2..p9.json`），`loadHistory()` 会合并全部 —— 只读首片做分析会得到**假低的命中率**
（我第一次模拟就是这样：18 个候选 17 个"查不到峰值"）。

顺带澄清一个普遍误解（已写进 README）：雷达卡片的迷你曲线是**按该词自己的峰值归一化**的（峰值恒 100），
所以**每条曲线看起来都很"猛"**，卡片之间不能比高低 —— 要看绝对量级只能看「分数明细」里的那两行。

### 16. 潜伏列表：修「走建站推荐评估」这个兑现不了的承诺 + 补 Steam 官方数据 + 链接置顶（2026-09-26）

用户三个反馈：① 潜伏列表里的「走建站推荐评估」是什么意思？在建站推荐里根本看不到；
② 似乎只有 Steam 比较全，希望补全其他数据；③ Steam 愿望单的原始链接能不能置顶。

#### ① 「走建站推荐评估」= 评估列的兜底文案，而且它**常常兑现不了**

- 那行文案来自 `assessCell()`：**潜伏评分只有 Roblox 未发售条目有**（发布确定性/日期/内容面/社区/竞争五维），
  App Store / Steam 两支没有 → 旧版一律写「走「🎯 建站推荐」评估」。
- **为什么"看不到"**：Steam 清单**只显示距发售 ≥7 天的条目**（`<7 天` 的那轮就"发售即转正"推进雷达队列、从清单消失），
  所以清单里看到的 Steam 条目**此刻必然不在雷达里**。实测线上：Steam 40 条、`stats.steamPromoted: 0`、**0 条在 games.json**。
- 修法（改完用真实数据跑了 103 条）：

| 情况 | 现在显示 | 线上条数 |
|---|---|---|
| 已在 games.json | 「在「🎯 建站推荐」里看 →」**可点击**（切页 + 按名字筛出它） | **34** |
| 未进但在转正窗口（临门 / 已发售 ≤14 天） | 「未进雷达（已发售 2 天：在转正窗口内，下一轮出现）」 | 32 |
| 未定档 / 超窗口 | 说清规则（`<7 天那轮才转正` / `超出 14 天窗口不会自动补入`） | 14 |
| Roblox 有潜伏评分 | 照旧显示 分数 + 分档（鼠标悬停看五维理由） | 23 |

- 配套：**建站推荐页新增「按游戏名筛」输入框**（深链就落在它上面）。

#### ② 补数据：Steam 官方数据先补上；App Store 的空值是真没有

- 线上 103 条的字段填充率：Steam 只有 `愿望单序位 40 · 发售日 40 · Demo 40 · 价格 33`，**评价数/好评率一行都没有**；
  App Store `榜单名次/发售日/价格` 齐全，但**评分只有 8/40** —— 其余是"刚上架还没人评"，**不是缺抓**（如实标注即可）。
- 本次：Steam 潜伏条目接 `enrichSteamStats()`（**复用雷达侧同一份实现、同一个 `.steam-stats-cache.json`、同一套 TTL 与限额**），
  取到**评价数 / 好评率 / 当前在线**并在行内显示 —— 不新增任何抓取代码。
- 🚧 **仍缺、需要用户定**：给 **Steam / App Store 做「潜伏评分」**（愿望单序位、发售窗口、Demo、榜单名次这些
  **上线前可测**的维度）。这是让"评估"在潜伏列表内部闭环的正解，但它是**一套新评分模型**（Roblox 那套五维是按
  Roblox 的可测项定的），建议单独一轮：先出设计，再实现。

#### ③ 链接置顶

- **游戏名变成链接**（Steam 商店 / App Store / Roblox 游戏页）—— 点名字直接去看愿望单、公告、评论；
- Steam 行加「**愿望单榜↗**」指向原始榜单页（`popularcomingsoon`）；
- 🛑 这些 URL 由**后端** `stats.sourceUrls` 下发（前端不手抄 URL，铁律 7）→ **下一轮采集后**才会出现（本次先把下发做好）。

#### 实测补充（本地跑通后）

- 潜伏清单 104 条（Steam 39 / Roblox 25 / iOS 40）；`Steam 潜伏条目官方数据：补 39 条（跳过 0 · 失败 0）`。
- 🛑 **但 39 条 Steam 条目全是"未发售"** → 评价数 / 在线**本来就是 0**（抓成功、真没有，不是失败）。
  所以这批数据的直接价值是**预填雷达侧的缓存**（`.steam-stats-cache.json` 共用 → 雷达下一轮少发约 117 次 appdetails 请求；
  实测本轮请求约 117 次）；等清单里出现已发售 / EA / 有 Demo 的条目，行内会立刻显示。
  行内显示因此改成「**有值才显示**」—— 不再打出「评分 — · 评价 0」这种噪音。
- 前端渲染实测（引擎 vm 直接调 `watchRowHtml` / `assessCell`）：
  `AION 2 … 愿望单#3 · 愿望单榜↗ … 商店页 · Trends · SERP`，名字是链接 ✓。

#### 🛑 顺带踩到并修掉一个会白屏的坑

最初把 `#pick-q` 的输入绑定写成 `debounce(...)`，而 `debounce` 是文件**后面**才定义的 `var`
→ **vm 里加载 app.js 直接抛 `debounce is not a function`（页面会白屏）**。已改成自管定时器。
教训：**顶层初始化代码不能引用后面才定义的 `var` 函数**；这类问题正是靠验收脚本（引擎 vm 加载 app.js）抓出来的 ——
所以"改前端必须过一遍 vm 加载"这条要保留。

### 17. 潜伏评分扩展：三来源各用可测维度（并修掉 Roblox 社区地基的权重口径 bug）（2026-09-26）

**背景 / 用户口径**：潜伏列表里 Steam（39 条）与 App Store（40 条）的评估列只会显示「未进雷达（…）」，
而用户真正想要的是「这两个来源该不该盯」→ 明确授权「你来做评分」。

#### 设计：新模块 `src/lib/upcoming.mjs`（三套维度 · 同一形状 · 同一套分档）

| 来源 | 维度（权重） |
|---|---|
| Roblox（原有，未改口径） | 发布确定性 28 · 日期精确度 18 · 内容面 14 · 社区地基 20 · 竞争饱和度 20 |
| **Steam（新）** | 愿望单热度 32（`popularcomingsoon` 序位）· 发售窗口 20（8~21 天甜区）· 可玩信号 14（Demo / 预购）· 官方热度 14（评价/在线/好评率）· 日期精确度 10 · 竞争 10 |
| **App Store（新）** | 榜单名次 34（× 榜单类型权重）· 新鲜度 22（预购 / 刚上架）· 口碑证据 24（评分人数 + 星级）· 内容面 10 · 竞争 10 |

返回结构与 Roblox 那套**完全一致**（`{ score, band, reasons, missing }`）→ 前端零改动即可显示（同一渲染路径）。

#### 三个必须记的决策

1. **覆盖三来源**：实测 **104/104 条全部有分**（此前 79 条只能显示「未进雷达」）。
2. **删掉恒定项**：「开发者」在 40/40 条里都有值 = **常数**，只会整体抬分、不产生区分度 ——
   与雷达分删掉「识别权重 ×2」**同一条理由**（榜单里恒定的输入是噪音，不是信号）。只写进理由、不计分。
3. **按来源校准门槛**：App Store 上调到 `≥78 / ≥62`。理由：那张清单是**新上架小榜**（`new-paid` / `new-free`），
   天然满足「刚上架 + 榜前 40」→ 名次与新鲜度接近满分。

#### 微调过程（先测后调，不猜）

- 第一版实测：Steam `10 / 19 / 10`（值得潜伏/观察/暂不）✓ 合理；**App Store `38 / 2 / 0`** ✗ —— 40 条里 38 条落进「值得潜伏」，该档等于失效。
- 定位：iOS 的三项输入天然偏高 —— 名次（小榜前 40）· 新鲜度（清单只收刚上架）· 开发者（常数 100）。
- 三处修：① 名次按**榜单类型**缩放（`new-*` 小榜 ×0.62，`top-free/top-grossing` 大榜 ×1.0 —— 名次只在同一张榜内可比）；
  ② **新鲜度锚点拉开**（≤2 天 100 / ≤7 天 86 / ≤21 天 68 / ≤60 天 45 —— 否则它本身也是常数）；
  ③ 开发者移出计分（见上）。
- 复测：App Store `3 / 30 / 7`（中位 68、区间 56~79）✓ 有区分度；三来源合计「值得潜伏」**48 → 19**。

#### 🛑 顺带修掉一个真 bug：Roblox「社区地基」的实际权重只有 4%

- 代码累加的是 **0~20 原始分**（Discord +12 · YouTube +5 · 群组 +3，封顶 20），却按 **0.20 权重**乘进总分
  → 实际贡献 `0.20 × 20/100 = 4%`，而规则文案写的是 **20%**（差 5 倍，且从代码与文案表面都看不出来）。
- 实测影响（线上 25 条 Roblox 条目）：**25/25 条分数都变 · 17 条跨档 · 平均 +7**
  （典型 `Beyond Nen` 56 → 76，观察 → 值得潜伏）。
- 修法：原始分**归一到 0~100**（×5），理由文本写明「原始分 0~20 → 归一到 0~100 后按 20% 权重计入」。
- 📌 教训：**权重与原始分的量纲必须一致** —— 这类错误只有"复算一遍"才会现形（文案和代码各自看都自洽）。

#### 前端

- `assessCell()`：兜底文案改成三来源口径；有评分但 `score == null`（各维全缺）时显示「数据不足（…）」而**不是 `null`**。
- 统计面板新增「**三来源潜伏评估**」块（已评分/未评分 · 按来源 · 按分档 · 平均分）；Roblox 那块保持原样（它标着「Roblox 未发售清单」）。
- 规则自述由后端**合并下发**（Roblox 五维 + 两个新来源的维度表），`rulesHtml()` 直接渲染 ——
  所以文案写在评分器旁边，改代码不改文案就会不一致（铁律 7）。

#### 实测（线上 104 条，本地跑通）

| 来源 | 条数 | 值得潜伏 | 观察 | 暂不 | 风险 | 中位分 | 区间 |
|---|---|---|---|---|---|---|---|
| Roblox | 25 | 6 | 18 | 0 | 1 | 64 | 48~79 |
| Steam | 39 | 10 | 19 | 10 | 0 | 61 | 43~89 |
| App Store | 40 | 3 | 30 | 7 | 0 | 68 | 56~79 |
| **合计** | 104 | **19** | **67** | **17** | 1 | — | 平均 65 |

> 🛑 App Store 的区间窄（56~79）是**清单本身同质**的真实反映（只收刚上架 + 榜前 40），不是打分没区分度；
> 🛑 **跨来源不能直接比大小**（三套维度不同）—— 迭代这条已写进规则自述、列表说明与 README。

## 相关文档

- `README.md` → 「体检与验收（两个自检命令）」· 「🎯 建站推荐」的七项评分与硬否决 · 「🚀 潜伏列表」
- `docs/DESIGN.md` → 铁律 5（多字段多生命周期）· **铁律 8（字段语义变了就得让旧数据失效，且所有读写点一致）**
- `.rankup/demand/game-review/2026-09-25-report.md` → 本轮选题复盘的原始证据（DW3 复刻版 / Dressmaker 两个对照样本）
